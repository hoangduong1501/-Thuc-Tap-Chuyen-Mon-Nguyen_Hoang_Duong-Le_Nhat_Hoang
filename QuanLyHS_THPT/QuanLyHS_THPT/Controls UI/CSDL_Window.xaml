<Window x:Class="QuanLyHS_THPT.Controls_UI.CSDL_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyHS_THPT.Controls_UI"
        WindowStartupLocation="CenterScreen"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="CSDL" Height="270" Width="350">
    <Grid>
        <ComboBox x:Name="cbb_SeverName"
                    Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                    materialDesign:HintAssist.Hint="Kết quả" Width="200" IsEditable="True"
                    VerticalAlignment="Top" HorizontalAlignment="Center" Margin="50,18,92,0"/>
        <Button x:Name="btn_Update"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Margin="0,18,39,0" Click="Chip_Click"
                    ToolTip="Kế tiếp" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Background="{DynamicResource MaterialDesignTextFieldBoxBackground}" TabIndex="7">
            <materialDesign:PackIcon Kind="Refresh" />
        </Button>
    </Grid>
</Window>
